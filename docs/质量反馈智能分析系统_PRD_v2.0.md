# 质量反馈智能分析系统 PRD

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档版本 | v2.0 |
| 创建日期 | 2026-02-26 |
| 更新日期 | 2026-02-26 |
| 状态 | 定稿 |
| 作者 | Kimi Claw |

---

## 1. 项目背景

### 1.1 业务背景
- **产品**：空调硬件产品
- **市场**：美国市场
- **客户**：美国终端用户 + 安装商
- **售后系统**：Zoho Desk 租户系统
- **反馈形式**：Tickets 登记质量反馈

### 1.2 现状痛点
| 痛点 | 影响 |
|------|------|
| 数据处理耗时 | 每周花大量时间洗数据，挤压问题分析时间 |
| 手工分类效率低 | 2000+ 条已分类，但人工跟不上新增速度 |
| 缺乏系统洞察 | 难以快速定位需要 QC 检讨的关键问题 |
| 响应滞后 | 批量问题发现晚，错过最佳处理窗口 |

### 1.3 核心目标
**不是做"数据分析系统"，而是构建"问题驱动的工作流"**

```
现在：每周 N 小时洗数据 → 没时间深挖 → 问题积压
目标：每周 30 分钟看简报 → 快速定位 → QC 检讨 → 质量改善
```

---

## 2. 产品定位

### 2.1 核心价值
| 维度 | 说明 |
|------|------|
| 用户 | 质量经理（主要使用者） |
| 场景 | 每周质量回顾、QC 小组启动、问题闭环跟踪 |
| 输出 | "本周质量简报"（飞书推送） |
| 价值 | 从"数据处理"转向"问题解决" |

### 2.2 成功标准
| 指标 | 目标 |
|------|------|
| 数据处理时间 | 每周 < 30 分钟 |
| 分类准确率 | ≥ 90% |
| QC 检讨触发 | 自动识别 ≥2 起的同类问题 |
| 简报生成 | 每周一早上自动推送 |

---

## 3. 业务规则

### 3.1 质量等级计算（综合评分）

```
优先级得分 = 严重度 × 发生次数 × 趋势系数
```

**严重度配置（可修改）：**

| 问题现象 | 严重度 | 说明 |
|---------|--------|------|
| 安全风险（漏电、起火） | 5 | 最高优先级 |
| 功能失效（不制冷/不制热） | 4 | 核心功能 |
| 漏水 | 4 | 财产损失风险 |
| 安装问题 | 3 | 影响性能 |
| 噪音大 | 3 | 影响体验 |
| 异味 | 3 | 健康担忧 |
| 显示故障 | 2 | 非核心功能 |
| 外观瑕疵 | 2 | 美观问题 |
| 其他 | 1 | 待分类 |

**趋势系数：**
- 环比增长 >100%：1.5
- 环比增长 50-100%：1.2
- 持平或下降：1.0

**等级划分：**
- 🔴 紧急：得分 ≥ 30 或 安全风险
- 🟡 关注：得分 15-29 或 新类型
- 🟢 跟踪：得分 < 15

### 3.2 同类问题定义
**维度：问题现象 + 机型**

示例：
- "Model-X + 不制冷" = 同类问题
- "Model-X + 噪音大" = 不同类问题

### 3.3 QC 检讨触发条件
- 同一（问题现象 + 机型）本周 ≥ 2 起
- 或 🔴 紧急等级问题

### 3.4 数据来源
- **一期**：Zoho Desk 导出 Excel → 本地导入
- **二期**：Zoho API 自动对接（可选）

---

## 4. 功能需求

### 4.1 模块架构

```
┌─────────────────────────────────────────────────────────────┐
│                    质量反馈智能分析系统                        │
├─────────────┬─────────────┬─────────────┬─────────────────────┤
│  数据导入层  │  数据处理层  │  分析展示层  │     预警通知层       │
├─────────────┼─────────────┼─────────────┼─────────────────────┤
│ Excel 导入  │ 数据清洗    │ 质量简报    │ 自动评分定级        │
│ 历史加载    │ 意图识别    │ 趋势图表    │ QC 触发提醒         │
│ 样本管理    │ 问题分类    │ 问题详情    │ 飞书推送            │
│             │ 置信度评估  │ 数据导出    │                     │
└─────────────┴─────────────┴─────────────┴─────────────────────┘
```

### 4.2 功能清单

#### M1: 数据导入模块

| 功能点 | 描述 | 优先级 |
|--------|------|--------|
| Excel 导入 | 支持 Zoho 导出格式直接导入 | P0 |
| 字段映射 | 自动识别/手动配置字段对应关系 | P0 |
| 历史加载 | 批量导入历史数据（2000+ 样本） | P0 |
| 增量导入 | 支持仅导入新增数据 | P1 |
| 数据验证 | 检查必填字段、格式校验 | P0 |

**Excel 字段要求：**
- 反馈内容（必填）
- 机型/型号（必填）
- 反馈时间（必填）
- 地区/州（可选）
- 来源（安装商/终端用户）（可选）
- 客户信息（可选）

#### M2: 数据处理模块

| 功能点 | 描述 | 优先级 |
|--------|------|--------|
| 数据清洗 | 去重、格式标准化、无效过滤 | P0 |
| 意图识别 | 理解反馈真实意图，提取关键词 | P0 |
| 问题分类 | 按预设标签自动分类 | P0 |
| 置信度评分 | 标注分类可信度（0-1） | P0 |
| 低置信度标记 | 置信度 < 0.75 标记需复核 | P0 |
| 人工复核 | 支持修改分类、补充标注 | P1 |
| 模型迭代 | 根据复核结果优化模型 | P2 |

**分类标签体系：**

| 一级分类 | 二级分类 | 关键词示例 |
|---------|---------|-----------|
| 功能失效 | 不制冷 | not cooling, warm air |
| 功能失效 | 不制热 | not heating, cold air |
| 功能失效 | 无法启动 | won't start, dead |
| 漏水 | 室内机漏水 | leaking, water drip |
| 漏水 | 室外机漏水 | outdoor leak |
| 噪音 | 外机噪音 | loud noise, vibration |
| 噪音 | 内机噪音 | fan noise, whistling |
| 安装问题 | 安装不当 | installation, mounted |
| 安装问题 | 管道问题 | piping, refrigerant |
| 显示故障 | 面板故障 | display, error code |
| 外观瑕疵 | 外观损坏 | scratch, dent |
| 异味 | 异味 | smell, odor |
| 其他 | - | - |

#### M3: 分析展示模块

| 功能点 | 描述 | 优先级 |
|--------|------|--------|
| 质量简报 | 每周自动生成核心指标 | P0 |
| 问题列表 | 按优先级排序的问题清单 | P0 |
| 趋势图表 | 各类型问题时间趋势 | P0 |
| 机型分布 | 问题在机型上的分布 | P0 |
| 地区分布 | 问题在地区上的分布 | P1 |
| 问题详情 | 单问题的完整信息展示 | P0 |
| 数据导出 | Excel/CSV 导出 | P0 |

**简报内容示例：**

```
【本周质量简报】2026.02.24-03.02

━━━━━━━━━━━━━━━━━━━━
🔴 紧急（2项需 QC 检讨）
━━━━━━━━━━━━━━━━━━━━
1. 【不制冷 | Model-X Pro】得分: 48
   数量: 8起 | 上周: 3起 | 趋势: ↑167%
   地区: 佛罗里达(4) 德州(2) 加州(2)
   来源: 安装商6 + 终端2
   [查看详情] [发起 QC]

━━━━━━━━━━━━━━━━━━━━
🟡 关注（1项）
━━━━━━━━━━━━━━━━━━━━
2. 【噪音大 | Model-Y】得分: 18
   数量: 3起 | 上周: 0起 | 趋势: 新增
   关键词: "外机异响""夜间明显"

━━━━━━━━━━━━━━━━━━━━
📊 本周概览
━━━━━━━━━━━━━━━━━━━━
总反馈: 42起 | 需 QC 检讨: 2项 | 新增类型: 1项
TOP3: 不制冷(8) | 噪音(5) | 漏水(4)
```

#### M4: 预警通知模块

| 功能点 | 描述 | 优先级 |
|--------|------|--------|
| 自动评分 | 根据规则自动计算优先级得分 | P0 |
| QC 触发提醒 | 自动识别需检讨的问题 | P0 |
| 简报推送 | 每周一早上飞书推送 | P0 |
| 紧急预警 | 🔴 紧急问题即时推送 | P0 |
| 通知配置 | 支持配置接收人和渠道 | P1 |

---

## 5. 技术方案

### 5.1 技术栈

| 层级 | 选型 | 说明 |
|------|------|------|
| 数据导入 | pandas + openpyxl | Excel 处理 |
| 数据存储 | SQLite / PostgreSQL | 本地存储 |
| 分类模型 | bert-base-chinese | 中文/英文混合支持 |
| 训练框架 | transformers + sklearn | 模型微调 |
| 后端服务 | FastAPI | API 服务 |
| 前端界面 | Streamlit | 快速开发 |
| 简报生成 | Jinja2 + Markdown | 模板渲染 |
| 消息通知 | 飞书 Webhook | 推送简报 |
| 部署 | 本地 Python 环境 | 无需 Docker |

### 5.2 部署方式

**方案：纯本地 Python 应用**

```
┌─────────────────────────────────────────┐
│           用户笔记本（i7+32G+4070）       │
│  ┌─────────────────────────────────┐    │
│  │     质量反馈分析系统（Python）     │    │
│  │  ┌─────────┐ ┌─────────┐       │    │
│  │  │ Streamlit│ │ 定时任务 │       │    │
│  │  │  界面   │ │ APScheduler│      │    │
│  │  └─────────┘ └─────────┘       │    │
│  │  ┌─────────┐ ┌─────────┐       │    │
│  │  │ SQLite  │ │ 模型文件 │       │    │
│  │  │  数据   │ │ bert-base│       │    │
│  │  └─────────┘ └─────────┘       │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
```

**启动方式：**
```bash
python main.py
# 或
streamlit run app.py
```

### 5.3 数据模型

```sql
-- 原始反馈表
CREATE TABLE feedback_raw (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    ticket_id VARCHAR(50),
    subject TEXT,
    content TEXT NOT NULL,
    model VARCHAR(100),
    region VARCHAR(100),
    source_type VARCHAR(20), -- installer / end_user
    customer_info TEXT,
    created_at TIMESTAMP,
    imported_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 分类结果表
CREATE TABLE feedback_classified (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    feedback_id INTEGER REFERENCES feedback_raw(id),
    primary_category VARCHAR(50),
    secondary_category VARCHAR(50),
    severity INTEGER, -- 1-5
    confidence FLOAT,
    keywords TEXT,
    intent_summary TEXT,
    needs_review BOOLEAN DEFAULT FALSE,
    reviewed_by VARCHAR(50),
    reviewed_at TIMESTAMP,
    classified_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 周统计表
CREATE TABLE weekly_stats (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    week_start DATE,
    week_end DATE,
    total_count INTEGER,
    category_stats JSON,
    model_stats JSON,
    alert_count INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- QC 检讨记录表
CREATE TABLE qc_review (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    category VARCHAR(50),
    model VARCHAR(100),
    feedback_ids TEXT, -- JSON array
    count INTEGER,
    severity_score INTEGER,
    status VARCHAR(20), -- pending / in_progress / resolved
    assigned_to VARCHAR(50),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    resolved_at TIMESTAMP
);
```

---

## 6. 实施计划

### 6.1 里程碑

```
M1: 基础框架（1周）
├── 项目结构搭建
├── 数据库设计
├── Excel 导入功能
└── 基础界面

M2: 智能分类（2周）
├── 数据清洗流程
├── 分类模型训练（基于 2000+ 样本）
├── 置信度评估
└── 人工复核界面

M3: 简报生成（1周）
├── 评分算法实现
├── 简报模板设计
├── 飞书推送对接
└── 定时任务

M4: 测试优化（1周）
├── 端到端测试
├── 准确率调优
├── 用户体验优化
└── 文档编写
```

**总计：5 周 MVP 可用**

### 6.2 交付物

| 阶段 | 交付物 |
|------|--------|
| M1 | 可导入 Excel 的基础系统 |
| M2 | 可自动分类的测试版本 |
| M3 | 可生成简报的完整流程 |
| M4 | 生产可用版本 + 使用文档 |

---

## 7. 使用流程

### 7.1 每周操作流程

```
周一早上
    ↓
系统生成简报（自动）
    ↓
飞书推送简报
    ↓
你查看简报，确认 QC 检讨项
    ↓
发起 QC 小组（系统外）
    ↓
周五前
    ↓
从 Zoho 导出本周新数据
    ↓
导入系统
    ↓
人工复核低置信度项（如有）
    ↓
完成
```

### 7.2 首次使用流程

```
1. 准备 2000+ 样本 Excel（含分类标签）
2. 导入系统作为训练数据
3. 训练分类模型
4. 验证准确率
5. 配置飞书 Webhook
6. 导入本周新数据测试
7. 正式上线
```

---

## 8. 验收标准

### 8.1 功能验收

| 验收项 | 标准 |
|--------|------|
| Excel 导入 | 支持 Zoho 导出格式，无错误 |
| 分类准确率 | 测试集 ≥ 90% |
| 评分计算 | 符合业务规则 |
| 简报生成 | 每周一自动推送，内容完整 |
| QC 触发 | 正确识别 ≥2 起的同类问题 |

### 8.2 性能验收

| 验收项 | 标准 |
|--------|------|
| 数据处理 | 100 条 < 10 秒 |
| 简报生成 | < 30 秒 |
| 界面响应 | < 3 秒 |

---

## 9. 待确认事项

- [ ] 2000+ 样本数据格式确认
- [ ] 严重度配置确认
- [ ] 飞书 Webhook 配置
- [ ] 简报推送时间（周一早上几点？）
- [ ] 是否需支持英文反馈分类

---

## 10. 附录

### 10.1 术语表

| 术语 | 解释 |
|------|------|
| QC 小组 | 质量控制小组，跨部门问题分析解决团队 |
| 置信度 | 模型对分类结果的确定程度（0-1） |
| 简报 | 每周质量状况总结报告 |
| 同类问题 | 相同问题现象 + 相同机型 |

### 10.2 文件清单

| 文件 | 说明 |
|------|------|
| main.py | 主程序入口 |
| app.py | Streamlit 界面 |
| models/ | 分类模型目录 |
| data/ | 数据库目录 |
| config.yaml | 配置文件 |
| requirements.txt | 依赖清单 |

---

**文档结束**
